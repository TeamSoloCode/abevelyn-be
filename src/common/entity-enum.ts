export enum SignInType {
  FACEBOOK = 'facebook',
  GOOGLE = 'google',
  REGISTER = 'register',
}

export enum UserRoles {
  ROOT = 'root',
  ADMIN = 'admin',
  USER = 'user',
}

export enum LanguageCode {
  VIETNAMESE = 'vn',
  FRENCH = 'fr',
  ENGLISH = 'en',
}
